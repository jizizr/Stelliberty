<div align="center">

# 🌟 Stelliberty

[![한국어](https://img.shields.io/badge/한국어-red)](README.ko.md)
[![简体中文](https://img.shields.io/badge/简体中文-blue)](README.zh-CN.md)
[![English](https://img.shields.io/badge/English-blue)](../../README.md)
[![日本語](https://img.shields.io/badge/日本語-blue)](README.ja.md)
[![Deutsch](https://img.shields.io/badge/Deutsch-blue)](README.de.md)
[![Français](https://img.shields.io/badge/Français-blue)](README.fr.md)

![정식 버전](https://img.shields.io/github/v/release/Kindness-Kismet/Stelliberty?style=flat-square&label=%EC%A0%95%EC%8B%9D%20%EB%B2%84%EC%A0%84)![최신 버전](https://img.shields.io/github/v/tag/Kindness-Kismet/Stelliberty?style=flat-square&label=%EC%B5%9C%EC%8B%A0%20%EB%B2%84%EC%A0%84&color=orange)![Flutter](https://img.shields.io/badge/Flutter-3.38%2B-02569B?style=flat-square&logo=flutter)![Rust](https://img.shields.io/badge/Rust-1.91%2B-orange?style=flat-square&logo=rust)![License](https://img.shields.io/badge/license-Stelliberty-green?style=flat-square)

![Windows](https://img.shields.io/badge/Windows-0078D6?style=flat-square&logo=windows11&logoColor=white)![Linux](https://img.shields.io/badge/Linux-FCC624?style=flat-square&logo=linux&logoColor=black)![macOS](https://img.shields.io/badge/macOS-실험적-gray?style=flat-square&logo=apple&logoColor=white)![Android](https://img.shields.io/badge/Android-미지원-lightgray?style=flat-square&logo=android&logoColor=white)

Flutter와 Rust로 개발된 세련된 크로스플랫폼 Clash 클라이언트입니다.
독자적인 **MD3M**(Material Design 3 Modern) 비주얼 스타일을 채택했습니다.

</div>

## 📸 애플리케이션 스크린샷

<table>
  <tr>
    <td width="50%"><img src="../../.github/screenshots/home-page.jpg" alt="홈페이지"/></td>
    <td width="50%"><img src="../../.github/screenshots/uwp-loopback-manager.jpg" alt="UWP 루프백 관리자"/></td>
  </tr>
  <tr>
    <td align="center"><b>홈페이지</b></td>
    <td align="center"><b>UWP 루프백 관리자</b></td>
  </tr>
</table>

---

## ✨ 특징

- 🎨 **MD3M 디자인 시스템**: MD3의 색상 시스템과 반투명 유리(Frosted Glass) 효과를 결합한 독창적인 Material Design 3 Modern 스타일.
- 🦀 **Rust 백엔드**: 고성능 Rust 코어를 기반으로 하며, UI는 Flutter로 렌더링합니다.
- 🌐 **다국어 지원**: slang 프레임워크를 통해 손쉽게 다국어를 지원합니다.
- 🔧 **구독 관리**: 구독 및 프로필 규칙(Override) 설정을 완벽하게 지원합니다.
- 📊 **실시간 모니터링**: 연결 및 트래픽 사용량을 실시간으로 추적하고 통계를 제공합니다.
- 🪟 **네이티브 데스크톱 통합**: Windows 서비스, 시스템 트레이, 시작 프로그램 등록을 완벽 지원합니다.
- 🔄 **UWP 루프백 관리자 내장**: Windows UWP 앱의 루프백 통신을 허용하는 도구를 내장했습니다 (Windows 전용).

### 🏆 구현 하이라이트

이 앱은 세심한 디테일이 돋보이는 최고의 Flutter 데스크톱 애플리케이션 중 하나입니다.

- ✨ **시스템 트레이 테마 지원**: Windows 트레이 아이콘이 시스템의 라이트/다크 테마에 자동으로 적응합니다.
- 🚀 **깜박임 없는 실행**: 창을 최대화해도 실행 시 화면 깜박임이 없습니다.
- 👻 **부드러운 창 전환**: 창을 열고 닫을 때 애니메이션이 매끄럽습니다.
- 🎯 **픽셀 퍼펙트 UI**: 한 픽셀의 오차도 없이 정교하게 설계된 MD3M 디자인 시스템.

---

## 📋 사용자 가이드

### 시스템 요구 사항

- **Windows**: Windows 10/11 (x64 / arm64)
- **Linux**: 주요 배포판 (x64 / arm64)
- **macOS**: 실험적

> ⚠️ **플랫폼별 지원 현황**: Windows와 Linux에서는 안정적으로 작동합니다. macOS 지원은 실험 단계로, 일부 기능이 불안정할 수 있습니다.

### 설치 방법

**다운로드 옵션:**
- **안정 버전**: [Releases](https://github.com/Kindness-Kismet/stelliberty/releases)
- **베타 버전**: [미리보기 릴리스](https://github.com/Kindness-Kismet/stelliberty/releases?q=prerelease%3Atrue) (최신 기능을 먼저 경험해보세요)

**설치 방법 (Windows):**

#### 방법 1: 포터블 버전 (ZIP 압축 파일)
1. 릴리스 페이지에서 `.zip` 파일을 다운로드합니다.
2. 원하는 위치에 압축을 풉니다 (예: `D:\Stelliberty`).
3. 압축 해제한 폴더에서 `stelliberty.exe`를 직접 실행하세요.
4. ✅ 별도의 설치 과정 없이 바로 사용할 수 있습니다.

#### 방법 2: 설치 프로그램 (EXE)
1. 릴리스 페이지에서 `.exe` 설치 프로그램을 다운로드합니다.
2. 설치 프로그램을 실행하고 마법사에 따라 설치를 완료합니다.
3. 설치 위치를 선택합니다 (아래 제한 사항 참조).
4. 바탕 화면 바로 가기에서 앱을 시작합니다.
5. ✅ 제거 프로그램과 바탕 화면 바로 가기가 포함됩니다.

**설치 디렉토리 제한 사항:**

안전성과 안정성을 보장하기 위해 설치 프로그램은 설치 경로에 다음과 같은 제한을 둡니다:

- **시스템 드라이브 (일반적으로 C: 드라이브)**:
  - ✅ 설치 가능: `%LOCALAPPDATA%\Programs\*` (예: `C:\Users\사용자명\AppData\Local\Programs\Stelliberty`)
  - ❌ 설치 금지: 시스템 드라이브의 루트 디렉토리 (예: `C:\`)
  - ❌ 설치 금지: 시스템 드라이브의 다른 모든 경로
  
- **기타 드라이브 (D:, E: 등)**:
  - ✅ 완전 자유, 제한 없음
  - ✅ 루트 디렉토리에 설치 가능 (예: `D:\`, `E:\Stelliberty`)

> 💡 **권장**: 최적의 사용 환경을 위해, 예기치 않은 권한 문제를 피하려면 시스템 드라이브가 아닌 다른 드라이브(예: `D:\Stelliberty`, `E:\Apps\Stelliberty`)에 설치하는 것을 권장합니다.

> 📌 **참고**: 기본 설치 경로인 `%LOCALAPPDATA%\Programs\Stelliberty`는 별도의 관리자 권한이 필요 없어 대부분의 사용자에게 가장 편리한 선택입니다.

**설치 방법 (Linux):**

#### Arch Linux (AUR)
지원 아키텍처: `x86_64`, `aarch64`

`yay` 사용:
```bash
yay -S stelliberty-bin
```

`paru` 사용:
```bash
paru -S stelliberty-bin
```

> AUR 패키지 링크: [stelliberty-bin](https://aur.archlinux.org/packages/stelliberty-bin)

---

#### 포터블 버전 (ZIP 압축 파일)
1. 릴리스 페이지에서 아키텍처(`amd64` 또는 `arm64`)에 맞는 `.zip` 파일을 다운로드합니다.
2. 원하는 위치에 압축을 풉니다 (예: `~/Stelliberty`).
3. **중요:** 애플리케이션 폴더에 권한을 부여해야 합니다:
   ```bash
   chmod 777 -R ./stelliberty
   ```
4. 압축 해제한 폴더에서 `./stelliberty`를 직접 실행하세요.
5. ✅ 이제 바로 사용할 수 있습니다.

### 버그 리포트

문제나 버그를 발견하시면 아래 절차에 따라 알려주세요.

1. **설정** → **앱 동작** 메뉴에서 **앱 로그** 기능을 활성화합니다.
2. 버그를 재현하여 로그를 기록합니다.
3. 프로그램 실행 폴더 안의 `data` 폴더에서 로그 파일을 찾습니다.
4. 로그 파일에 포함된 개인정보나 민감한 정보를 모두 삭제합니다.
5. GitHub 이슈(Issue)에 정리된 로그 파일을 첨부하여 등록합니다.
6. 문제 상황과 재현 방법을 상세히 설명해주세요.

---

## 🛠️ 개발자 가이드

### 사전 요구 사항

이 프로젝트를 빌드하기 전에 다음 도구가 설치되어 있는지 확인하십시오:

- **Flutter SDK** (최신 안정 버전 권장, 최소 3.38)
- **Rust 툴체인** (최신 안정 버전 권장, 최소 1.91)
- **Dart SDK** (Flutter에 포함)

> 📖 이 가이드는 Flutter와 Rust 개발 환경에 익숙한 분들을 대상으로 합니다. 각 도구의 설치 방법은 따로 설명하지 않습니다.

### 의존성 설치

#### 1. 스크립트 의존성 설치

사전 빌드 스크립트에는 추가 Dart 패키지가 필요합니다:

```bash
cd scripts
dart pub get
```

#### 2. rinf CLI 설치

Rust-Flutter 브리지 도구를 전역으로 설치합니다:

```bash
cargo install rinf_cli
```

#### 3. 프로젝트 의존성 설치

```bash
flutter pub get
```

#### 4. 필수 코드 생성

의존성을 설치한 후 Rust-Flutter 브리지 코드와 국제화 번역 파일을 생성합니다:

```bash
# Rust-Flutter 브리지 코드 생성
rinf gen

# 국제화 번역 파일 생성
dart run slang
```

> 💡 **중요**: 첫 빌드 전, 반드시 아래 코드 생성 명령어를 실행해야 합니다.

### 프로젝트 빌드

#### 사전 빌드 준비

**프로젝트를 빌드하기 전에 반드시 사전 빌드 스크립트를 실행해야 합니다:**

```bash
dart run scripts/prebuild.dart
```

**사전 빌드 스크립트 매개변수:**

```bash
# 도움말 정보 표시
dart run scripts/prebuild.dart --help

# 플랫폼 패키징 도구 설치 (Windows: Inno Setup, Linux: dpkg/rpm/appimagetool)
dart run scripts/prebuild.dart --installer

# Android 지원 (아직 구현되지 않음)
dart run scripts/prebuild.dart --android
```

**사전 빌드 스크립트의 주요 기능:**

1. ✅ `assets` 리소스 폴더 정리 (`test/` 폴더는 제외).
2. ✅ `stelliberty-service` (데스크톱 서비스 모드용 실행 파일) 컴파일.
3. ✅ 각 OS에 맞는 트레이 아이콘 복사.
4. ✅ 최신 Mihomo 코어 바이너리 다운로드.
5. ✅ GeoIP/GeoSite 데이터 파일 다운로드.

#### 빠른 빌드

빌드 스크립트를 사용하여 컴파일 및 패키징:

```bash
# 도움말 정보 표시
dart run scripts/build.dart --help

# 릴리스 버전 빌드 (기본값: ZIP만)
dart run scripts/build.dart

# 디버그 버전도 함께 빌드
dart run scripts/build.dart --with-debug

# 설치 프로그램도 함께 생성 (Windows: ZIP + EXE, Linux: ZIP + DEB/RPM/AppImage)
dart run scripts/build.dart --with-installer

# ZIP 없이 설치 프로그램만 생성 (Windows: EXE, Linux: DEB/RPM/AppImage)
dart run scripts/build.dart --installer-only

# 전체 빌드 (릴리스 + 디버그, 설치 프로그램 포함)
dart run scripts/build.dart --with-debug --with-installer

# 클린 빌드
dart run scripts/build.dart --clean

# Android APK 빌드 (미지원)
dart run scripts/build.dart --android
```

**빌드 스크립트 매개변수:**

| 매개변수 | 설명 |
|------|------|
| `-h, --help` | 도움말 정보 표시 |
| `--with-debug` | 릴리스 및 디버그 버전을 함께 빌드 |
| `--with-installer` | ZIP + 설치 프로그램 생성 (Windows: EXE, Linux: DEB/RPM/AppImage) |
| `--installer-only` | ZIP 없이 설치 프로그램만 생성 |
| `--clean` | 빌드 전 `flutter clean` 실행 |
| `--android` | Android APK 빌드 (미지원) |

**출력 위치:**

빌드된 패키지는 `build/packages/` 디렉토리에 위치합니다.

#### 알려진 제한 사항

⚠️ **플랫폼 지원 상태**:

- ✅ **Windows**: 완전한 테스트 및 지원
- ⚠️ **Linux**: 핵심 기능은 사용 가능하지만 시스템 통합(서비스, 자동 시작)은 검증되지 않음
- ⚠️ **macOS**: 핵심 기능은 사용 가능하지만 시스템 통합은 실험적
- ❌ **Android**: 아직 구현되지 않음

⚠️ **사용할 수 없는 매개변수**:

- `--android`: Android 플랫폼은 아직 적용되지 않음

### 수동 개발 프로세스

#### Rust-Flutter 바인딩 생성

Rust 시그널 구조체(시그널 속성 포함)를 수정한 후:

```bash
rinf gen
```

> 📖 Rinf는 `.proto` 파일을 사용하는 대신, Rust 구조체(struct)에 시그널 속성을 추가하여 메시지를 정의합니다. 자세한 내용은 [Rinf 공식 문서](https://rinf.cunarist.com)를 참고하세요.

#### 국제화 번역 생성

`lib/i18n/strings/`의 번역 파일을 수정한 후:

```bash
dart run slang
```

#### 개발 빌드 실행

```bash
# 먼저 사전 빌드 실행
dart run scripts/prebuild.dart

# 개발 시작
flutter run
```

#### 개발 테스트

프로젝트에는 특정 기능을 격리하여 테스트하기 위한 테스트 프레임워크가 내장되어 있습니다:

```bash
# 덮어쓰기 규칙 테스트 실행 (YAML 또는 JS 규칙 지원)
flutter run --dart-define=TEST_TYPE=override

# IPC API 테스트 실행
flutter run --dart-define=TEST_TYPE=ipc-api
```

**필요한 테스트 파일**은 `assets/test/`에 있습니다:

- **`override` 테스트에 필요한 파일:**
  ```
  assets/test/
  ├── config/
  │   └── test.yaml          # 테스트용 기본 설정 파일
  ├── override/
  │   ├── your_script.js     # JS 덮어쓰기 스크립트
  │   └── your_rules.yaml    # YAML 덮어쓰기 규칙
  └── output/
      └── final.yaml         # 덮어쓰기 적용 후 예상되는 최종 출력 파일
  ```

- **`ipc-api` 테스트에 필요한 파일:**
  > **팁**: 테스트 전에 사전 빌드 스크립트(`dart run scripts/prebuild.dart`)를 실행하여 필요한 리소스를 다운로드하는 것이 좋습니다.
  ```
  assets/test/
  └── config/
      └── test.yaml          # 테스트용 기본 설정 파일
  ```

> 💡 **참고**: 테스트 모드는 디버그(Debug) 빌드에서만 활성화되며, 릴리스(Release) 빌드에서는 자동으로 비활성화됩니다.

테스트 구현: `lib/dev_test/` (`override_test.dart`, `ipc_api_test.dart`)

---

## ❓ 문제 해결

### 포트 사용 중 (Windows)

포트 충돌이 발생하면:

```bash
# 1. 포트를 점유하는 프로세스 찾기
netstat -ano | findstr :포트번호

# 2. 프로세스 종료 (관리자 권한으로 실행)
taskkill /F /PID XXX
```

> ⚠️ **중요**: 이 명령어는 관리자 권한으로 실행해야 합니다. 서비스 모드로 실행된 코어 프로세스는 일반 권한으로 종료할 수 없습니다.

### 소프트웨어가 제대로 작동하지 않음

**경로 요구 사항** (ZIP 및 EXE 모두 적용):

- 경로에 특수 문자(공백 제외)가 포함되어서는 안 됩니다.
- 경로에 한글과 같은 Non-ASCII 문자가 포함되지 않도록 주의해주세요.
- 공백은 지원됩니다: `D:\Program Files\Stelliberty` ✅

**EXE 설치 프로그램의 위치 제한**:

EXE 설치 프로그램을 사용하는 경우 추가적인 설치 위치 제한이 있습니다:

- **시스템 드라이브 (C: 드라이브)**: `%LOCALAPPDATA%\Programs\*`에만 설치 가능
- **기타 드라이브 (D:, E: 등)**: 제한 없음

> 💡 **팁**: EXE 설치 프로그램이 허용하지 않는 위치에 설치해야 하는 경우 **포터블 ZIP 압축 파일**을 사용하십시오. ZIP 버전은 위치 제한이 없지만 시스템 디렉토리 권한의 영향을 받을 수 있습니다(예: `C:\Windows` 또는 `C:\Program Files`에 압축을 풀면 관리자 권한이 필요할 수 있음).

### 런타임 라이브러리 누락 (Windows)

Windows에서 프로그램이 실행되지 않거나 즉시 종료된다면, Visual C++ 런타임 라이브러리가 설치되지 않았을 가능성이 높습니다.

**해결책:**

Visual C++ 런타임 라이브러리 설치: [vcredist - Visual C++ 런타임 라이브러리 모음](https://gitlab.com/stdout12/vcredist)

---

## 🎨 MD3M 디자인에 대하여

**MD3M(Material Design 3 Modern)**은 다음을 융합한 독특한 디자인 시스템입니다:

- 🎨 **Material Design 3**: 현대적인 색상 시스템과 타이포그래피
- 🪟 **프로스트 글래스 효과**: 반투명 배경과 블러 효과
- 🌈 **시스템 테마 통합**: 시스템 강조 색상에 자동 적응
- 🌗 **다크 모드 지원**: 원활한 라이트/다크 테마 전환

이를 통해 각 플랫폼에서 네이티브와 같은 부드러운 느낌을 주는 현대적이고 우아한 데스크톱 애플리케이션 경험을 제공합니다.

---

## 📋 코드 규약

- ✅ `flutter analyze`와 `cargo clippy`에서 경고가 없어야 합니다.
- ✅ 커밋 전 `dart format`과 `cargo fmt`로 코드를 포맷합니다.
- ✅ 자동 생성된 디렉토리(`lib/src/bindings/`, `lib/i18n/`)는 수정하지 않습니다.
- ✅ `setState`의 남용을 피하고, 이벤트 기반 아키텍처를 지향합니다.
- ✅ Rust 코드에서는 `unwrap()` 대신 `Result<T, E>`를 사용해 오류를 처리합니다.
- ✅ Dart 코드는 Null Safety를 준수해야 합니다.

---

## 📄 라이선스

이 프로젝트는 **Stelliberty License(별빛 자유 라이선스)**를 따릅니다. 자세한 내용은 [LICENSE](../../LICENSE) 파일을 참고하세요.

**한마디로**: 이 소프트웨어는 출처를 밝힐 필요 없이, 아무런 제약 없이 자유롭게 사용하실 수 있습니다.

---

<div align="center">

Flutter와 Rust의 힘으로.

</div>
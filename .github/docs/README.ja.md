<div align="center">

# 🌟 Stelliberty

[![日本語](https://img.shields.io/badge/日本語-red)](README.ja.md)
[![简体中文](https://img.shields.io/badge/简体中文-blue)](README.zh-CN.md)
[![English](https://img.shields.io/badge/English-blue)](../../README.md)
[![한국어](https://img.shields.io/badge/한국어-blue)](README.ko.md)
[![Deutsch](https://img.shields.io/badge/Deutsch-blue)](README.de.md)
[![Français](https://img.shields.io/badge/Français-blue)](README.fr.md)

![安定版](https://img.shields.io/github/v/release/Kindness-Kismet/Stelliberty?style=flat-square&label=%E5%AE%89%E5%AE%9A%E7%89%88)![最新版](https://img.shields.io/github/v/tag/Kindness-Kismet/Stelliberty?style=flat-square&label=%E6%9C%80%E6%96%B0%E7%89%88&color=orange)![Flutter](https://img.shields.io/badge/Flutter-3.38%2B-02569B?style=flat-square&logo=flutter)![Rust](https://img.shields.io/badge/Rust-1.91%2B-orange?style=flat-square&logo=rust)![License](https://img.shields.io/badge/license-Stelliberty-green?style=flat-square)

![Windows](https://img.shields.io/badge/Windows-0078D6?style=flat-square&logo=windows11&logoColor=white)![Linux](https://img.shields.io/badge/Linux-FCC624?style=flat-square&logo=linux&logoColor=black)![macOS](https://img.shields.io/badge/macOS-実験的-gray?style=flat-square&logo=apple&logoColor=white)![Android](https://img.shields.io/badge/Android-未対応-lightgray?style=flat-square&logo=android&logoColor=white)

Flutter と Rust で開発された、モダンなクロスプラットフォーム対応の Clash クライアント。
独自のビジュアルスタイル **MD3M** (Material Design 3 Modern) を採用しています。

</div>

## 📸 アプリケーションのスクリーンショット

<table>
  <tr>
    <td width="50%"><img src="../../.github/screenshots/home-page.jpg" alt="ホームページ"/></td>
    <td width="50%"><img src="../../.github/screenshots/uwp-loopback-manager.jpg" alt="UWP ループバックマネージャー"/></td>
  </tr>
  <tr>
    <td align="center"><b>ホームページ</b></td>
    <td align="center"><b>UWP ループバックマネージャー</b></td>
  </tr>
</table>

---

## ✨ 特徴

- 🎨 **MD3M デザインシステム**: MD3 のカラーシステムとアクリル風のすりガラス効果を融合させた、独自の Material Design 3 Modern スタイル。
- 🦀 **Rust バックエンド**: 高性能な Rust コアをバックエンドに、UI は Flutter で描画。
- 🌐 **多言語対応**: slang フレームワークによる国際化をビルトイン。
- 🔧 **サブスクリプション管理**: サブスクリプションとオーバーライド設定を完全にサポート。
- 📊 **リアルタイム監視**: 接続とトラフィックをリアルタイムで追跡・統計。
- 🪟 **ネイティブデスクトップ統合**: Windows サービス、システムトレイ、スタートアップに完全対応。
- 🔄 **UWP ループバックマネージャー**: Windows UWP アプリのループバック通信を許可するツールを内蔵 (Windows 限定)。

### 🏆 実装のハイライト

本アプリケーションは、細部まで作り込まれた、最高峰の Flutter デスクトップアプリケーションの一つです。

- ✨ **システムトレイのテーマ対応**: Windows のトレイアイコンが、システムのライト/ダークテーマに自動追従。
- 🚀 **フリッカーフリーな起動**: ウィンドウを最大化しても、起動時にちらつきが発生しません。
- 👻 **スムーズなウィンドウ遷移**: ウィンドウの表示・非表示アニメーションも滑らか。
- 🎯 **ピクセルパーフェクトな UI**: 細部までこだわり抜いた MD3M デザインシステム。

---

## 📋 ユーザーガイド

### システム要件

- **Windows**: Windows 10/11 (x64 / arm64)
- **Linux**: 主要なディストリビューション (x64 / arm64)
- **macOS**: 実験的

> ⚠️ **各プラットフォームの対応状況**: Windows と Linux では安定して動作します。macOS のサポートは実験的段階にあり、一部機能が不完全な場合があります。

### インストール方法

**ダウンロードオプション:**
- **安定版**: [Releases](https://github.com/Kindness-Kismet/stelliberty/releases)
- **プレリリース版**: [プレリリースページ](https://github.com/Kindness-Kismet/stelliberty/releases?q=prerelease%3Atrue) (最新機能をお試しいただけます)

**インストール方法 (Windows):**

#### 方法1: ポータブル版 (ZIP 圧縮ファイル)
1. リリースページから `.zip` ファイルをダウンロード
2. 任意の場所に解凍 (例: `D:\Stelliberty`)
3. 解凍したフォルダにある `stelliberty.exe` を直接実行します。
4. ✅ インストール不要。解凍するだけですぐに使えます。

#### 方法2: インストーラー (EXE)
1. リリースページから `.exe` インストーラーをダウンロード
2. インストーラーを実行し、ウィザードに従ってインストールを完了
3. インストール場所を選択 (以下の制限事項を参照)
4. デスクトップのショートカットからアプリケーションを起動
5. ✅ アンインストーラーとデスクトップショートカットを含む

**インストールディレクトリの制限事項:**

安全性と安定性を確保するため、インストーラーはインストールパスに以下の制限を設けています:

- **システムドライブ (通常は C: ドライブ)**:
  - ✅ インストール可能: `%LOCALAPPDATA%\Programs\*` (例: `C:\Users\ユーザー名\AppData\Local\Programs\Stelliberty`)
  - ❌ インストール禁止: システムドライブのルートディレクトリ (例: `C:\`)
  - ❌ インストール禁止: システムドライブの他のすべてのパス
  
- **その他のドライブ (D:, E: など)**:
  - ✅ 完全に自由、制限なし
  - ✅ ルートディレクトリへのインストールも可能 (例: `D:\`、`E:\Stelliberty`)

> 💡 **推奨**: 最高の体験を得るには、システムドライブ以外 (例: `D:\Stelliberty`, `E:\Apps\Stelliberty`) へのインストールを推奨します。これにより、予期せぬ権限問題を回避できます。

> 📌 **補足**: デフォルトのインストール先 `%LOCALAPPDATA%\Programs\Stelliberty` は特別な権限が不要なため、多くのユーザーにとって最適な選択です。

**インストール方法 (Linux):**

#### Arch Linux (AUR)
対応アーキテクチャ: `x86_64`、`aarch64`

`yay` を使用:
```bash
yay -S stelliberty-bin
```

`paru` を使用:
```bash
paru -S stelliberty-bin
```

> AUR パッケージリンク: [stelliberty-bin](https://aur.archlinux.org/packages/stelliberty-bin)

---

#### ポータブル版 (ZIP 圧縮ファイル)
1. リリースページからお使いのアーキテクチャ (`amd64` または `arm64`) に適した `.zip` ファイルをダウンロード
2. 任意の場所に解凍 (例: `~/Stelliberty`)
3. **重要:** アプリケーションフォルダに権限を付与します:
   ```bash
   chmod 777 -R ./stelliberty
   ```
4. 解凍したディレクトリで `./stelliberty` を直接実行します。
5. ✅ これだけで、すぐに使用できます。

### 不具合報告

不具合や問題が発生した場合は、以下の手順でご報告ください。

1. **設定** → **アプリの動作** にある **アプリのログ** 機能を有効にします。
2. 問題を再現させ、ログを記録します。
3. アプリケーションの実行ディレクトリ内にある `data` フォルダからログファイルを探します。
4. ログファイルから個人情報や機密情報を削除します。
5. GitHub で Issue を作成し、編集後のログファイルを添付してください。
6. 問題の内容と再現手順を詳しく記述してください。

---

## 🛠️ 開発者ガイド

### 前提条件

このプロジェクトをビルドする前に、以下のツールがインストールされていることを確認してください:

- **Flutter SDK** (最新の安定版を推奨、最低 3.38)
- **Rust ツールチェーン** (最新の安定版を推奨、最低 1.91)
- **Dart SDK** (Flutter に付属)

> 📖 本ガイドは、Flutter と Rust の開発環境に慣れていることを前提としています。各ツールのインストール方法は省略します。

### 依存関係のインストール

#### 1. スクリプトの依存関係をインストール

ビルド前スクリプトには追加の Dart パッケージが必要です:

```bash
cd scripts
dart pub get
```

#### 2. rinf CLI をインストール

Rust-Flutter ブリッジツールをグローバルにインストール:

```bash
cargo install rinf_cli
```

#### 3. プロジェクトの依存関係をインストール

```bash
flutter pub get
```

#### 4. 必要なコードを生成

依存関係をインストールした後、Rust-Flutter ブリッジコードと国際化翻訳ファイルを生成します:

```bash
# Rust-Flutter ブリッジコードを生成
rinf gen

# 国際化翻訳ファイルを生成
dart run slang
```

> 💡 **重要**: 初回ビルド前には、必ずこれらのコード生成コマンドを実行してください。

### プロジェクトのビルド

#### ビルド前の準備

**プロジェクトをビルドする前に、必ずビルド前スクリプトを実行してください:**

```bash
dart run scripts/prebuild.dart
```

**ビルド前スクリプトのパラメータ:**

```bash
# ヘルプ情報を表示
dart run scripts/prebuild.dart --help

# プラットフォームのパッケージングツールをインストール (Windows: Inno Setup, Linux: dpkg/rpm/appimagetool)
dart run scripts/prebuild.dart --installer

# Android サポート (未実装)
dart run scripts/prebuild.dart --android
```

**ビルド前スクリプトの主な処理内容:**

1. ✅ `assets` ディレクトリをクリーンアップ (ただし `test/` フォルダは保持)。
2. ✅ `stelliberty-service` (デスクトップ版サービスモード用バイナリ) をコンパイル。
3. ✅ 各プラットフォームに対応したトレイアイコンをコピー。
4. ✅ 最新の Mihomo コアバイナリをダウンロード。
5. ✅ GeoIP/GeoSite データファイルをダウンロード。

#### クイックビルド

ビルドスクリプトを使用してコンパイルとパッケージング:

```bash
# ヘルプ情報を表示
dart run scripts/build.dart --help

# Release 版をビルド (デフォルト: ZIP のみ)
dart run scripts/build.dart

# 同時に Debug 版もビルド
dart run scripts/build.dart --with-debug

# 同時にインストーラーも生成 (Windows: ZIP + EXE, Linux: ZIP + DEB/RPM/AppImage)
dart run scripts/build.dart --with-installer

# インストーラーのみ生成、ZIP なし (Windows: EXE, Linux: DEB/RPM/AppImage)
dart run scripts/build.dart --installer-only

# フルビルド (Release + Debug, インストーラーを含む)
dart run scripts/build.dart --with-debug --with-installer

# クリーンビルド
dart run scripts/build.dart --clean

# Android APK をビルド (未対応)
dart run scripts/build.dart --android
```

**ビルドスクリプトのパラメータ:**

| パラメータ | 説明 |
|------|------|
| `-h, --help` | ヘルプ情報を表示 |
| `--with-debug` | Release 版と Debug 版を同時にビルド |
| `--with-installer` | ZIP + インストーラーを生成 (Windows: EXE, Linux: DEB/RPM/AppImage) |
| `--installer-only` | インストーラーのみ生成、ZIP なし |
| `--clean` | ビルド前に `flutter clean` を実行 |
| `--android` | Android APK をビルド (未対応) |

**出力場所:**

ビルドされたパッケージは `build/packages/` ディレクトリに配置されます

#### 既知の制限

⚠️ **プラットフォームのサポート状況**:

- ✅ **Windows**: 完全なテストとサポート
- ⚠️ **Linux**: コア機能は利用可能ですが、システム統合 (サービス、自動起動) は未検証
- ⚠️ **macOS**: コア機能は利用可能ですが、システム統合は実験的
- ❌ **Android**: 未実装

⚠️ **利用できないパラメータ**:

- `--android`: Android プラットフォームはまだ対応していません

### 手動開発フロー

#### Rust-Flutter バインディングの生成

Rust のシグナル構造体 (シグナル属性付き) を変更した後:

```bash
rinf gen
```

> 📖 Rinf では `.proto` ファイルの代わりに、Rust の struct に付けられたシグナル属性でメッセージを定義します。詳細は [Rinf ドキュメント](https://rinf.cunarist.com) をご参照ください。

#### 国際化翻訳の生成

`lib/i18n/strings/` 内の翻訳ファイルを変更した後:

```bash
dart run slang
```

#### 開発ビルドの実行

```bash
# まずビルド前スクリプトを実行
dart run scripts/prebuild.dart

# 開発を開始
flutter run
```

#### 開発テスト

プロジェクトには、特定の機能を分離してテストするためのテストフレームワークが組み込まれています:

```bash
# オーバーライドルールのテストを実行 (YAML または JS ルールをサポート)
flutter run --dart-define=TEST_TYPE=override

# IPC API のテストを実行
flutter run --dart-define=TEST_TYPE=ipc-api
```

**必要なテストファイル** は `assets/test/` にあります:

- **`override` テストに必要なファイル:**
  ```
  assets/test/
  ├── config/
  │   └── test.yaml          # テスト用の基本設定ファイル
  ├── override/
  │   ├── your_script.js     # JS オーバーライドスクリプト
  │   └── your_rules.yaml    # YAML オーバーライドルール
  └── output/
      └── final.yaml         # オーバーライド適用後の期待される最終出力ファイル
  ```

- **`ipc-api` テストに必要なファイル:**
  > **ヒント**: テスト前にビルド前スクリプト (`dart run scripts/prebuild.dart`) を実行して、必要なリソースをダウンロードすることをお勧めします。
  ```
  assets/test/
  └── config/
      └── test.yaml          # テスト用の基本設定ファイル
  ```

> 💡 **注意**: テストモードは Debug ビルドでのみ有効です。Release ビルドでは自動的に無効化されます。

テストの実装: `lib/dev_test/` (`override_test.dart`、`ipc_api_test.dart`)

---

## ❓ トラブルシューティング

### ポートが使用中 (Windows)

ポートの競合が発生した場合:

```bash
# 1. ポートを占有しているプロセスを検索
netstat -ano | findstr :ポート番号

# 2. プロセスを終了 (管理者として実行)
taskkill /F /PID XXX
```

> ⚠️ **重要**: このコマンドは管理者権限で実行する必要があります。サービスモードで起動したコアプロセスを強制終了するには、昇格された権限が求められます。

### ソフトウェアが正常に動作しない

**パスの要件** (ZIP と EXE の両方に適用):

- パスに特殊文字 (スペースを除く) を含めないでください
- パスに非 ASCII 文字 (日本語の文字など) を含めないでください。
- スペースはサポートされています: `D:\Program Files\Stelliberty` ✅

**EXE インストーラーの場所の制限**:

EXE インストーラーを使用する場合、追加のインストール場所の制限があります:

- **システムドライブ (C: ドライブ)**: `%LOCALAPPDATA%\Programs\*` にのみインストール可能
- **その他のドライブ (D:, E: など)**: 制限なし

> 💡 **ヒント**: インストーラーが許可しないカスタムパスに配置したい場合は、**ポータブル版 (ZIP)** をご利用ください。ZIP 版にはインストール先の制限はありませんが、`C:\Windows` や `C:\Program Files` のようなシステムディレクトリに配置すると、権限の問題が発生する可能性があります。

### ランタイムライブラリが不足 (Windows)

Windows でアプリケーションが起動しない、または即座にクラッシュする場合、必要な Visual C++ ランタイムが不足している可能性があります。

**解決策:**

Visual C++ ランタイムライブラリをインストール: [vcredist - Visual C++ ランタイムライブラリコレクション](https://gitlab.com/stdout12/vcredist)

---

## 🎨 MD3M デザインについて

**MD3M (Material Design 3 Modern)** は、以下を融合したユニークなデザインシステムです:

- 🎨 **Material Design 3**: モダンなカラーシステムとタイポグラフィ
- 🪟 **すりガラス効果**: 半透明の背景とぼかし効果
- 🌈 **システムテーマ統合**: システムのアクセントカラーに自動的に適応
- 🌗 **ダークモード対応**: シームレスなライト/ダークテーマの切り替え

これにより、各プラットフォームでネイティブのようなスムーズな感覚を持つ、モダンでエレガントなデスクトップアプリケーション体験が生まれます。

---

## 📋 コーディング規約

- ✅ `flutter analyze` と `cargo clippy` の警告ゼロを維持。
- ✅ コミット前には `dart format` と `cargo fmt` でコードを整形。
- ✅ 自動生成ディレクトリ (`lib/src/bindings/`, `lib/i18n/`) は変更しない。
- ✅ イベント駆動アーキテクチャを採用し、`setState` の多用を避ける。
- ✅ Rust のエラー処理には `Result<T, E>` を使用し、`unwrap()` は禁止。
- ✅ Dart コードでは Null Safety を徹底。

---

## 📄 ライセンス

このプロジェクトは **Stelliberty License (星々の自由ライセンス)** の下で公開されています。詳細は [LICENSE](../../LICENSE) ファイルをご覧ください。

**要約**: このソフトウェアは、作者の表示も不要で、完全に自由にご利用いただけます。

---

<div align="center">

Flutter と Rust の力で。

</div>